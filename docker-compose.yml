services:
  mysql:
    image: mysql:8.4
    container_name: mysql-db-fastapi
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
      MYSQL_DATABASE: ${DB_NAME}
    ports:
      - "3307:3306"